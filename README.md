# TypeScript: Interface vs Type

TypeScript-এ **interface** এবং **type alias**—দুটোই অবজেক্ট, ফাংশন, ক্লাস ইত্যাদির স্ট্রাকচার সংজ্ঞায়িত করতে ব্যবহার হয়। তবে কিছু গুরুত্বপূর্ণ পার্থক্য রয়েছে, যা জানা থাকলে কোড আরও ভালোভাবে সংগঠিত করা যায়। এই ডকুমেন্টে সহজ বাংলা ভাষায় তাদের মূল পার্থক্য, ব্যবহার এবং উদাহরণ তুলে ধরা হলো।

---

## 🔍 Interface কী?
**Interface** হলো একটি কাঠামো বা চুক্তি, যেখানে নির্দিষ্ট কিছু প্রপার্টি এবং মেথড কেমন হবে তা নির্ধারণ করা হয়। এটি প্রধানত অবজেক্টের গঠন বর্ণনা করতে ব্যবহৃত হয়।

**উদাহরণ:**
```ts
interface User {
  name: string;
  age: number;
}
```

### Interface-এর সুবিধা
- **Extend করা সহজ** (Inheritance)।
- একই নামে একাধিকবার ডিক্লেয়ার করলে TypeScript সেগুলো **merge** করে।
- ক্লাসে implement করা যায়।

---

## 🔍 Type কী?
**Type alias** দিয়ে যেকোনো টাইপকে একটি কাস্টম নাম দেওয়া যায়। এটি শুধু অবজেক্ট নয়; ইউনিয়ন টাইপ, টিউপল, ফাংশন, প্রিমিটিভ—সবকিছু সংজ্ঞায়িত করতে পারে।

**উদাহরণ:**
```ts
type User = {
  name: string;
  age: number;
};
```

### Type-এর সুবিধা
- **Union** এবং **Intersection** টাইপ সহজে তৈরি করা যায়।
- জটিল টাইপ (যেমন: conditional types, mapped types) তৈরি করার ক্ষেত্রে বেশি শক্তিশালী।
- প্রিমিটিভ টাইপের জন্যও alias তৈরি করা যায়।

---

## 🔧 কবে Interface ব্যবহার করবেন?
- যখন আপনি অবজেক্টের গঠন নির্ধারণ করছেন।
- যখন আপনার কোডে ক্লাস ব্যবহার করছেন এবং `implements` করতে চান।
- যখন একাধিকবার একই interface ডিক্লেয়ার করে structure merge করার প্রয়োজন আছে।

**উদাহরণ:**
```ts
interface Person { name: string; }
interface Person { age: number; }

// Merge করে:
// Person = { name: string; age: number }
```

---

## 🔧 কবে Type ব্যবহার করবেন?
- Union টাইপ লাগলে (যেমন: `string | number`)
- Function টাইপ নির্ধারণ করতে
- Complex বা advanced টাইপ তৈরি করতে
- Tuples ও mapped types ব্যবহার করতে

**উদাহরণ:**
```ts
type Status = "success" | "error" | "pending";
```

---

## 🎯 সারসংক্ষেপ
সংক্ষেপে, Interface হলো অবজেক্ট ও ক্লাসের গঠন বর্ণনার জন্য উপযুক্ত, আর Type হলো আরও জেনেরিক, শক্তিশালী এবং বহুমুখী। আধুনিক TypeScript-এ দুটোই প্রায় একই রকম ক্ষমতাশালী, তবে **Interface = contract for structure**, আর **Type = flexible alias for any type**।

আপনি কোনটি ব্যবহার করবেন তা নির্ভর করে পরিস্থিতি এবং আপনার কোড স্টাইলের উপর।

